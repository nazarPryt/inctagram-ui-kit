import { Meta, IconGallery, IconItem } from '@storybook/blocks'

import * as Icons from './../icons'

<Meta title="System/Icons" />

# Icons

<IconGallery>
  {Object.entries(Icons).map(([name, Icon]) => (
    <IconItem name={name}>
      <Icon />
    </IconItem>
  ))}
</IconGallery>

[//]: # 'https://storybook.js.org/docs/api/doc-block-icongallery'
